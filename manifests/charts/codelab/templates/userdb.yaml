apiVersion: mongodbcommunity.mongodb.com/v1
kind: MongoDBCommunity
metadata:
  name: {{ .Values.userdb.name }}
  namespace: {{ .Release.Namespace }}
spec:
  members: {{ .Values.userdb.members }}
  type: ReplicaSet
  version: "{{ .Values.userdb.version }}"
  featureCompatibilityVersion: "{{ .Values.userdb.compatibilityVersion }}"
  security:
    authentication:
      ignoreUnknownUsers: true
      modes: ["SCRAM"]
    tls:
      certificateKeySecretRef: {}
      enabled: false
      optional: false
  users:
    - name: {{ .Values.userdb.auth.rootUsername }}
      db: admin
      passwordSecretRef:
        name: {{ .Values.userdb.auth.rootAuthSecretName }}
      roles:
        - name: clusterAdmin
          db: admin
        - name: userAdminAnyDatabase
          db: admin
      scramCredentialsSecretName: {{ .Values.userdb.auth.rootAuthSecretName }}
      connectionStringSecretName: {{ .Values.userdb.auth.rootConnectionStringSecretName }}
    - name: {{ .Values.userdb.auth.username }}
      db: admin
      passwordSecretRef:
        name: {{ .Values.userdb.auth.userAuthSecretName }}
      roles:
        - name: readWrite
          db: {{ .Values.userdb.database }}
      scramCredentialsSecretName: {{ .Values.userdb.auth.userAuthSecretName }}
      connectionStringSecretName: {{ .Values.userdb.auth.userConnectionStringSecretName }}
  statefulSet:
    spec:
      volumeClaimTemplates:
        - metadata:
            name: data-volume
          spec:
            accessModes: ["ReadWriteOnce"]
            resources:
              requests:
                storage: {{ .Values.userdb.storageSize }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.userdb.auth.rootAuthSecretName }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  password: {{ .Values.userdb.auth.rootPassword | b64enc }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.userdb.auth.userAuthSecretName }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  password: {{ .Values.userdb.auth.password | b64enc }}